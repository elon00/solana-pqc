name: Deprecated Devnet Deployment (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Devnet deployment is intentionally disabled."
          echo "SCSTOBCMinority AI is Testnet-first. Use .github/workflows/deploy-testnet.yml."
